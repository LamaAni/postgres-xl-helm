# Transient example deployment  

This example creates a transient deployment which will not persist data between restarts.

Alter values as required then sync with helmfile,
```shell
helmfile sync
```